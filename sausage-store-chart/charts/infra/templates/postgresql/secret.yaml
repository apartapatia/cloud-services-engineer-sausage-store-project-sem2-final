{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-secret
  labels:
    app: postgresql
type: Opaque
data:
  postgres-password: {{ default "" .Values.postgresql.env.POSTGRES_PASSWORD | b64enc | quote }}
{{- end }}
