{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-init
  labels:
    app: postgresql
data:
  init.sh: |
{{ .Files.Get "files/init.sh" | indent 4 }}
{{- end }}
